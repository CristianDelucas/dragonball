<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
        <title>{% block title %}PERSONAJES{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body class="bg-image" 
     style="background-image: url('https://es.dragon-ball-official.com/assets/img/shared/bg_comics_pc.jpg');
            height: 100vh" >
        <div class="container">

        
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
                <a class="navbar-brand" href="#"><img height="35px" src="{# LOGO #}"></a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                    <div class="navbar-nav">
                        <a class="nav-link active" href="{{ path('listPersonajes') }}">Inicio <span class="sr-only">(current)</span></a>
                        <a class="nav-link" href="{{ path('listPlanetas') }}">Planetas</a>
                        <a class="nav-link" href="{{ path('listPersonajes') }}">Personajes</a>
                        {% if is_granted('ROLE_ADMIN') %}
                        <a class="nav-link" href="{{ path('createPersonaje') }}">Añadir personaje</a>
                        {% endif %}
                        <a class="nav-link" href="#">Universos</a>
                        <a class="nav-link" href="#">Simular Torneo</a>
                        
                    </div>
                </div>
                <div class="my-2 my-lg-0">
                    {% if is_granted('ROLE_ADMIN') %}
                    <a class="btn btn-outline-secondary my-2 my-sm-0" href="#">Gestión usuarios</a>
                    {% endif %}
                    <a class="btn btn-outline-danger my-2 my-sm-0" href="/logout" >Desconectar</a>
                </div>
            </nav>
            <div class="container" style="padding:5rem 0px">
                {% block body %}{% endblock %}
            </div>
            <footer class="text-center text-white fixed-bottom" style={% if is_granted('ROLE_ADMIN') %}"background-color: red;"{% endif %}"background-color: #343A40;">
                <!-- Grid container -->
                <div class="container p-4"></div>
                <!-- Grid container -->

                <!-- Copyright -->
                <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2);">
                    © 2021 Copyright: 
                    <a class="text-white" href="#">Cristian</a>
                </div>
                <!-- Copyright -->
            </footer>
            {% block js %} {% endblock %}
        </div>
    </body>
</html>
